<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kingbo.petserver.dao.ShopDao">

    <resultMap type="com.kingbo.petserver.entity.Shop" id="ShopMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="tel" column="tel" jdbcType="VARCHAR"/>
        <result property="registerTime" column="register_time" jdbcType="TIMESTAMP"/>
        <result property="state" column="state" jdbcType="INTEGER"/>
        <result property="address" column="address" jdbcType="VARCHAR"/>
        <result property="logo" column="logo" jdbcType="VARCHAR"/>
        <result property="adminId" column="admin_id" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ShopMap">
        select id,
               name,
               tel,
               register_time,
               state,
               address,
               logo,
               admin_id
        from t_shop
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ShopMap">
        select
        *
        from t_shop
        <where>
            <if test="name != null and name != ''">
                name like concat('%',#{name},'%')
            </if>
        </where>
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from t_shop
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="tel != null and tel != ''">
                and tel = #{tel}
            </if>
            <if test="registerTime != null">
                and register_time = #{registerTime}
            </if>
            <if test="state != null">
                and state = #{state}
            </if>
            <if test="address != null and address != ''">
                and address = #{address}
            </if>
            <if test="logo != null and logo != ''">
                and logo = #{logo}
            </if>
            <if test="adminId != null">
                and admin_id = #{adminId}
            </if>
        </where>
    </select>

    <!-- 根据不同的店铺审核状态 统计每个状态的数量 -->
    <select id="selectShopGroupState" resultType="com.kingbo.petserver.vo.ShopStateEcharts">
        SELECT
            state as auditState,
            COUNT(id) as stateNum
        FROM
            t_shop
        GROUP BY
            state
    </select>


    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into t_shop(name, tel, register_time, state, address, logo, admin_id)
        values (#{name}, #{tel}, #{registerTime}, #{state}, #{address}, #{logo}, #{adminId})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into t_shop(name, tel, register_time, state, address, logo, admin_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.tel}, #{entity.registerTime}, #{entity.state}, #{entity.address}, #{entity.logo},
            #{entity.adminId})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into t_shop(name, tel, register_time, state, address, logo, admin_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.tel}, #{entity.registerTime}, #{entity.state}, #{entity.address}, #{entity.logo},
            #{entity.adminId})
        </foreach>
        on duplicate key update
        name = values(name),
        tel = values(tel),
        register_time = values(register_time),
        state = values(state),
        address = values(address),
        logo = values(logo),
        admin_id = values(admin_id)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update t_shop
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="tel != null and tel != ''">
                tel = #{tel},
            </if>
            <if test="registerTime != null">
                register_time = #{registerTime},
            </if>
            <if test="state != null">
                state = #{state},
            </if>
            <if test="address != null and address != ''">
                address = #{address},
            </if>
            <if test="logo != null and logo != ''">
                logo = #{logo},
            </if>
            <if test="adminId != null">
                admin_id = #{adminId},
            </if>
        </set>
        where id = #{id}
    </update>
    <update id="updateStateById">
        update t_shop set state = #{state} where id = {id}
    </update>

    <update id="updateAdminIdByAdminId">
        update t_shop set admin_id = null where admin_id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from t_shop
        where id = #{id}
    </delete>
    <delete id="deleteByIds">
        delete
        from t_shop
        where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

</mapper>

